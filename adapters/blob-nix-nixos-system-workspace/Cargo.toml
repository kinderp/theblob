[package]
name = "blob-nix-nixos-system-workspace"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Composition adapter from backend-neutral System Workspace proposals to NixOS candidate preparation"

[dependencies]
blob-nix-nixos = { path = "../../backends/blob-nix-nixos" }
blob-nix-nixos-candidate-producer = { path = "../../backends/blob-nix-nixos-candidate-producer" }
blob-system-workspace = { path = "../../crates/blob-system-workspace" }
