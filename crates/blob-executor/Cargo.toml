[package]
name = "blob-executor"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

description = "Controlled MVP execution backend for ephemeral The Blob capabilities"

[dependencies]
blob-core = { path = "../blob-core" }
