[package]
name = "blob-mvp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

description = "Vertical-slice harness proving the first The Blob workflow"

[dependencies]
blob-core = { path = "../blob-core" }
blob-alfred = { path = "../blob-alfred" }
blob-resolver = { path = "../blob-resolver" }
blob-executor = { path = "../blob-executor" }
blob-history = { path = "../blob-history" }
