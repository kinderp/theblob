[package]
name = "blob-alfred"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

description = "Deterministic event normalization/correlation prototype for The Blob"

[dependencies]
blob-core = { path = "../blob-core" }
