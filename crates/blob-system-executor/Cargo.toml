[package]
name = "blob-system-executor"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Non-privileged system candidate executor for The Blob Linux Pilot"

[dependencies]
blob-core = { path = "../blob-core" }
blob-nix-nixos = { path = "../../backends/blob-nix-nixos" }
