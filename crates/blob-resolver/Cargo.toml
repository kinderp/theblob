[package]
name = "blob-resolver"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

description = "Deterministic MVP resolver and independent binding verifier for The Blob"

[dependencies]
blob-core = { path = "../blob-core" }
