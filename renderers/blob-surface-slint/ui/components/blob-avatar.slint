import { PixelBlobAvatar } from "pixel-blob-avatar.slint";

// Reversible P0 experiment.
// The previous soft-3D implementation is preserved in git history at
// f4ecd1029ebfa7642658f1cc5c457ca940d3bae4 and can be restored independently
// of Workspace/Surface semantics or the rest of the Shell composition.
export component BlobAvatar inherits PixelBlobAvatar { }
