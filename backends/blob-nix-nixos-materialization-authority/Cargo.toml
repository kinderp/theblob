[package]
name = "blob-nix-nixos-materialization-authority"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Root-owned derivation-bound materialization admission for NixOS"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-nix-nixos-request-publisher = { path = "../blob-nix-nixos-request-publisher" }
