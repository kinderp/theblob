[package]
name = "blob-nix-nixos-privileged-helper"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Narrow privileged runtime boundary for approved immutable NixOS activation"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-nix-nixos-activation = { path = "../blob-nix-nixos-activation" }
blob-system-activation-gate = { path = "../../crates/blob-system-activation-gate" }
