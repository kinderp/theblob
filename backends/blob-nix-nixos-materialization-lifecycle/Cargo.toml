[package]
name = "blob-nix-nixos-materialization-lifecycle"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Fail-closed lifecycle and GC-root management for NixOS materialization"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-nix-nixos-candidate-producer = { path = "../blob-nix-nixos-candidate-producer" }
blob-nix-nixos-materialization-authority = { path = "../blob-nix-nixos-materialization-authority" }
blob-nix-nixos-materialization-begin = { path = "../blob-nix-nixos-materialization-begin" }
blob-nix-nixos-materialization-begin-queue = { path = "../blob-nix-nixos-materialization-begin-queue" }
blob-nix-nixos-request-publisher = { path = "../blob-nix-nixos-request-publisher" }
