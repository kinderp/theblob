[package]
name = "blob-nix-nixos"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Minimal deterministic NixOS backend for The Blob SystemSpec"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
