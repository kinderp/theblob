[package]
name = "blob-nix-nixos-authority"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Polkit authority planning and root-owned activation permit issuance for NixOS"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-system-activation-gate = { path = "../../crates/blob-system-activation-gate" }
blob-nix-nixos-privileged-helper = { path = "../blob-nix-nixos-privileged-helper" }
blob-nix-nixos-root-boundary = { path = "../blob-nix-nixos-root-boundary" }
