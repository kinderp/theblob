[package]
name = "blob-nix-nixos-candidate-lease"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Durable enqueue leases for trusted NixOS candidate source liveness"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
