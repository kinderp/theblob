[package]
name = "blob-nix-nixos-candidate-source-reclaimer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Lease-aware fail-closed reclamation of retired NixOS candidate sources"

[dependencies]
blob-nix-nixos-candidate-lease = { path = "../blob-nix-nixos-candidate-lease" }
blob-nix-nixos-candidate-producer = { path = "../blob-nix-nixos-candidate-producer" }
blob-nix-nixos-materialization-begin = { path = "../blob-nix-nixos-materialization-begin" }
blob-nix-nixos-materialization-begin-queue = { path = "../blob-nix-nixos-materialization-begin-queue" }
blob-nix-nixos-materialization-lifecycle = { path = "../blob-nix-nixos-materialization-lifecycle" }
