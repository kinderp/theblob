[package]
name = "blob-nix-nixos-materialization-begin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Trusted candidate manifest and root materialization begin boundary for NixOS"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-nix-nixos-materialization-authority = { path = "../blob-nix-nixos-materialization-authority" }
