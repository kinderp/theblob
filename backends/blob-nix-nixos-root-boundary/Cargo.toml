[package]
name = "blob-nix-nixos-root-boundary"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Root-owned permit boundary for exact approved NixOS activation"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-system-activation-gate = { path = "../../crates/blob-system-activation-gate" }
blob-nix-nixos-activation = { path = "../blob-nix-nixos-activation" }
blob-nix-nixos-privileged-helper = { path = "../blob-nix-nixos-privileged-helper" }
