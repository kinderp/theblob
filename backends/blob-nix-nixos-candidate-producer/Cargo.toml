[package]
name = "blob-nix-nixos-candidate-producer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic SystemSpec to trusted NixOS candidate manifest producer"

[dependencies]
blob-core = { path = "../../crates/blob-core" }
blob-nix-nixos = { path = "../blob-nix-nixos" }
blob-nix-nixos-materialization-begin = { path = "../blob-nix-nixos-materialization-begin" }
